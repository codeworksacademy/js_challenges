
// a Hero needs name, health, maxHealth, exp (experience), and 2 getters for healthPercent and level

// Note that the hero only takes in 2 parameters for name and health, you will have to assume the values for maxHealth, and exp (experience)

class Hero {
  constructor(name, health) {

  }
}

